# Retired — historical record only

These are the original Phase 0-2 build specs (2026-03), moved here from the
top-level `specs/` directory on 2026-07-24 to free that path for GitHub Spec
Kit's per-feature specs.

On 2026-07-24 every document in this directory was cross-referenced against
the actual current code by a dedicated audit. Result: the large majority
accurately describe what was built and is still there — safe to treat as a
reliable historical record of what Phase 0-2 actually delivered. The
specific gaps and discrepancies that audit found were folded into
`docs/BACKLOG.md`, not left implicit in these files.

Do not add new documents here. For current work, see `docs/BACKLOG.md`. For
new feature specs, use GitHub Spec Kit (`specs/`, via `/speckit-specify`).

Full audit: `docs/findings/2026-07-24-plan-implementation-audit.md`
