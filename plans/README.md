# Retired — historical record only

These are the original Phase 0-2 implementation plans (2026-03), the
task-by-task companions to the design specs now in
`docs/design/legacy-specs/`.

On 2026-07-24 every document here was cross-referenced against the actual
current code by a dedicated audit, alongside its paired design spec.
Result: the large majority accurately describe what was built and is still
there — safe to treat as a reliable historical record. The specific gaps
and discrepancies that audit found were folded into `docs/BACKLOG.md`, not
left implicit in these files.

Do not add new documents here. For current work, see `docs/BACKLOG.md`. For
new feature specs, use GitHub Spec Kit (`specs/`, via `/speckit-specify`).

Full audit: `docs/findings/2026-07-24-plan-implementation-audit.md`
