//! Vexilc-generated code — all VNP domain modules.
//!
//! The generated code is included directly in `lib.rs` via `include!()` rather
//! than through this module. This is necessary because vexilc generates
//! cross-module imports using `crate::malt::*` paths, which requires the
//! `malt` module to exist directly under the crate root.
//!
//! See `build.rs` for the codegen invocation.
