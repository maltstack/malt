[package]
name = "malt-plugin-sdk"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "WASM plugin SDK for MALT — plugin loading, sandbox, fuel budgets"

[dependencies]
wasmtime = "29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
wat = "1"
